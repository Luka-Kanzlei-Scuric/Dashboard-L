# Beispiel-Umgebungsvariablen für die Anwendung
# Kopiere diese Datei zu .env.local und passe die Werte an

# Frontend-Konfiguration
VITE_API_URL=http://localhost:5000/api

# Backend-Konfiguration
# MongoDB connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/databasename

# Server configuration
PORT=5000

# Email configuration for sending notifications
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-password
EMAIL_FROM=Scuric Rechtsanwälte <no-reply@example.com>

# For testing emails (optional)
TEST_EMAIL=your-test-email@example.com

# Client portal URL for emails
CLIENT_PORTAL_BASE_URL=https://portal.example.com