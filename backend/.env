# Redis Configuration (intentionally invalid to force in-memory mode)
REDIS_URL=invalid-redis-url-to-force-fallback

# PowerDialer Settings
ENABLE_MOCK_MODE=true
FORCE_COMPLETE_MOCK=true

# Other necessary environment variables (placeholder values)
# Replace these with actual values if needed
MONGODB_URI=your-mongodb-connection-string
JWT_SECRET=your-jwt-secret
AIRCALL_API_KEY=your-aircall-api-key
